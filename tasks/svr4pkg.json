{
  "description": "Manage Solaris SVR4 packages", 
  "parameters": {
    "category": {
      "type": "Optional[String]", 
      "description": "Install/Remove category instead of a single package."
    }, 
    "src": {
      "type": "Optional[String]", 
      "description": "Specifies the location to install the package from. Required when C(state=present). Can be any path acceptable to the C(pkgadd) command's C(-d) option. e.g.: C(somefile.pkg), C(/dir/with/pkgs), C(http:/server/mypkgs.pkg). If using a file or directory, they must already be accessible by the host. See the M(copy) module for a way to get them there."
    }, 
    "name": {
      "type": "String", 
      "description": "Package name, e.g. C(SUNWcsr)"
    }, 
    "zone": {
      "type": "Optional[String]", 
      "description": "Whether to install the package only in the current zone, or install it into all zones. The installation into all zones works only if you are working with the global zone."
    }, 
    "state": {
      "type": "String", 
      "description": "Whether to install (C(present)), or remove (C(absent)) a package. If the package is to be installed, then I(src) is required. The SVR4 package system doesn't provide an upgrade operation. You need to uninstall the old, then install the new package."
    }, 
    "proxy": {
      "type": "Optional[String]", 
      "description": "HTTP[s] proxy to be used if C(src) is a URL."
    }, 
    "response_file": {
      "type": "Optional[String]", 
      "description": "Specifies the location of a response file to be used if package expects input on install. (added in Ansible 1.4)"
    }
  }
}