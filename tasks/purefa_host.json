{
  "description": "Create, Delete and Modify Hosts on Pure Storage FlashArray", 
  "parameters": {
    "protocol": {
      "type": "Optional[String]", 
      "description": "Defines the host connection protocol for volumes."
    }, 
    "state": {
      "type": "Optional[String]", 
      "description": "Creates host. When removing host all connected volumes will be disconnected."
    }, 
    "volume": {
      "type": "Optional[String]", 
      "description": "Volume name to map to the host"
    }, 
    "host": {
      "type": "String", 
      "description": "Host Name"
    }, 
    "iqn": {
      "type": "Optional[String]", 
      "description": "List of IQNs of the host if protocol is iscsi"
    }, 
    "api_token": {
      "type": "String", 
      "description": "FlashArray API token for admin privilaged user."
    }, 
    "fa_url": {
      "type": "String", 
      "description": "FlashArray management IPv4 address or Hostname."
    }, 
    "wwns": {
      "type": "Optional[String]", 
      "description": "List of wwns of the host if protocol is fc"
    }
  }
}