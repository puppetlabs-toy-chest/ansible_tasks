{
  "description": "Manages hosts on Apache CloudStack based clouds.", 
  "parameters": {
    "username": {
      "type": "Optional[String]", 
      "description": "Username for the host. Required if C(state=present) and host does not yet exist."
    }, 
    "api_key": {
      "type": "Optional[String]", 
      "description": "API key of the CloudStack API."
    }, 
    "name": {
      "type": "String", 
      "description": "Name of the host."
    }, 
    "api_http_method": {
      "type": "Optional[String]", 
      "description": "HTTP method used."
    }, 
    "host_tags": {
      "type": "Optional[String]", 
      "description": "Tags of the host."
    }, 
    "url": {
      "type": "Optional[String]", 
      "description": "Url of the host used to create a host. If not provided, C(http://) and param C(name) is used as url. Only considered if C(state=present) and host does not yet exist."
    }, 
    "api_secret": {
      "type": "Optional[String]", 
      "description": "Secret key of the CloudStack API."
    }, 
    "api_timeout": {
      "type": "Optional[String]", 
      "description": "HTTP timeout."
    }, 
    "api_region": {
      "type": "Optional[String]", 
      "description": "Name of the ini section in the C(cloustack.ini) file."
    }, 
    "cluster": {
      "type": "Optional[String]", 
      "description": "Name of the cluster."
    }, 
    "zone": {
      "type": "Optional[String]", 
      "description": "Name of the zone in which the host should be deployed. If not set, default zone is used."
    }, 
    "state": {
      "type": "Optional[String]", 
      "description": "State of the host."
    }, 
    "hypervisor": {
      "type": "Optional[String]", 
      "description": "Name of the cluster. Required if C(state=present) and host does not yet exist."
    }, 
    "pod": {
      "type": "Optional[String]", 
      "description": "Name of the pod. Required if C(state=present) and host does not yet exist."
    }, 
    "password": {
      "type": "Optional[String]", 
      "description": "Password for the host. Required if C(state=present) and host does not yet exist."
    }, 
    "allocation_state": {
      "type": "Optional[String]", 
      "description": "Allocation state of the host."
    }, 
    "api_url": {
      "type": "Optional[String]", 
      "description": "URL of the CloudStack API e.g. https://cloud.example.com/client/api."
    }
  }
}