{
  "description": "Manages regions on Apache CloudStack based clouds.", 
  "parameters": {
    "endpoint": {
      "type": "Optional[String]", 
      "description": "Endpoint URL of the region. Required if C(state=present)"
    }, 
    "api_url": {
      "type": "Optional[String]", 
      "description": "URL of the CloudStack API e.g. https://cloud.example.com/client/api."
    }, 
    "api_http_method": {
      "type": "Optional[String]", 
      "description": "HTTP method used."
    }, 
    "api_secret": {
      "type": "Optional[String]", 
      "description": "Secret key of the CloudStack API."
    }, 
    "api_timeout": {
      "type": "Optional[String]", 
      "description": "HTTP timeout."
    }, 
    "api_region": {
      "type": "Optional[String]", 
      "description": "Name of the ini section in the C(cloustack.ini) file."
    }, 
    "state": {
      "type": "Optional[String]", 
      "description": "State of the region."
    }, 
    "api_key": {
      "type": "Optional[String]", 
      "description": "API key of the CloudStack API."
    }, 
    "id": {
      "type": "String", 
      "description": "ID of the region. Must be an number (int)."
    }, 
    "name": {
      "type": "Optional[String]", 
      "description": "Name of the region. Required if C(state=present)"
    }
  }
}